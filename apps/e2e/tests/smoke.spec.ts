import { expect, test } from '@playwright/test';

/**
 * Smoke test — proves the Playwright harness runs end-to-end in CI
 * before any product app exists. Replace with real flows once apps/web lands.
 */
test('harness runs', () => {
  expect(true).toBe(true);
});
