/**
 * @ai-workspace-lab/auth — scaffold only.
 *
 * Authentication and session management. Wraps Supabase Auth (or Clerk if migrated per ADR).
 *
 * Implementation deferred. See docs/product/PRD-ai-workspace-saas.md for scope.
 */
export {};
