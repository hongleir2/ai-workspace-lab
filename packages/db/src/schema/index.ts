// No product tables yet.
// Schema files are added here as one file per domain, e.g.:
//   import * as organizations from './organizations.js';
//   export { organizations };
//
// See packages/db/README.md for schema conventions (naming, IDs, timestamps, RLS, pgvector).
// The first product migration (organizations + memberships) will land in a follow-up PR.
