/**
 * Shared UI primitives for ai-workspace-lab apps.
 *
 * Convention: components live one-per-file under `src/`, re-exported here.
 * Keep dependencies minimal — apps own their styling stack.
 */

export {};
